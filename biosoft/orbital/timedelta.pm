package orbital;

sub make_timedelta {
	my ($class) = shift;

	my $self = {};	
	return bless $self, $class;
}

sub make_tick
{
	my ($class) = shift;
	my $milliseconds = make_timedelta(;
	my $seconds = shift;
	my $interval = shift;	

		
